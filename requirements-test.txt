# The Home Assistant custom-integration test harness. It pulls in a
# matching core, pytest, pytest-asyncio and aiohttp, so this is the whole
# list. Deliberately unpinned: the point of the lane is to notice when a
# new core breaks this integration, which a pin would hide until someone
# remembered to bump it.
pytest-homeassistant-custom-component
